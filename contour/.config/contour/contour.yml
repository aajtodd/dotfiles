profiles:
  main:
    shell: "/bin/zsh"
    environment:
      TERM: contour
      COLORTERM: truecolor
    font:
      size: 12
      locator: native
      regular:
        family: "JetBrainsMono Nerd Font"
