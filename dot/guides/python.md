# python — uv package/script runner

```
uv run script.py     run with PEP 723 inline deps auto-installed
uv venv              create a .venv
uv pip install <pkg> fast pip
uvx <tool>           run a tool in an ephemeral env (pipx-run style)
```
